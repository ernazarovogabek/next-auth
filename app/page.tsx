"use client"

const Home = () => {
  // const randomNumber = Math.floor(Math.random() * 10);


  // if(randomNumber > 5) {
  //   throw new Error ("Random number 5 dan katta");

  // }
  return (
    <div>Home</div>
  )
}

export default Home